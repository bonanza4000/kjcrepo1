version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 8
  build:
    commands:
      - echo Nothing to do yet
artifacts:
  files:
      - index.html
      - error.html
      - favicon.ico
      - KennethJCartwrightResume.pdf
      - styles/*
      - images/*
      - data/*
