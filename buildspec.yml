version: 0.1

phases:
  install:
    runtime-versions:
      nodejs: 8
  build:
    commands:
      - echo Nothing to do yet
artifacts:
  files:
      - index.html
      - favicon.ico
      - KennethJCartwrightResume.pdf
      - styles/main.css
      - images/*
